{% extends "base.twig" %}

{% block content %}
    <div>
        {% if registerResult %}
            Congratulation, You've registed successuflly. An email is sent to you for activation code
            {% else %}
        <form action="#" method="post" class="form-signin">
            <h2 class="form-signin-heading">Please enter email address or telephone number</h2>
            {{ form_widget(form) }}
        </form>
        {% endif %}
    </div>
{% endblock %}
